class Solution {
    public int reverse(int x) {
        int newNum = 0;
        while( x != 0 )
        {
            int rem = x % 10;
            if( rem != 0 )
            {
                newNum = newNum * 10 + rem;
            }
            x = x/10;
        }
        return newNum;
    }
}
